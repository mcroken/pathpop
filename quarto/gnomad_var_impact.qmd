---
title: "gnomad impact"
author: "Matt Croken"
format: beamer
editor: visual
output: beamer_presentation
params:
  project_dir: "/usr/local/data/work"
---

```{r,echo=FALSE,include=FALSE}
library(tidyverse, quietly = TRUE)
library(vroom, quietly = TRUE)
library(ggplot2, quietly = TRUE)
```

## Read My Table

```{r}
oncokb_gnomad_file <- str_c(params$project_dir,"/oncoKB/gnomad.exomes.r2.1.1.sites.oncokb.plot.tbl.gz")
oncokb_gnomad <- vroom(
  delim = "\t",
  file = oncokb_gnomad_file,
  col_names = TRUE,
  col_select = 1:6,
  show_col_types = TRUE
)
```

```{r}
oncokb_gnomad_p <- ggplot(oncokb_gnomad,aes(impact,AF)) +
  geom_violin(aes(fill = impact )) +
  scale_y_log10()
oncokb_gnomad_p
```

```{r}
oncokb_gnomad %>%
  filter(str_detect(X94,"[A-z]"))
```
